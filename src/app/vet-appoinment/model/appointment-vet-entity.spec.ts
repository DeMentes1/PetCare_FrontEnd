import { AppointmentVetEntity } from './appointment-vet-entity';

describe('AppointmentVetEntity', () => {
  it('should create an instance', () => {
    expect(new AppointmentVetEntity()).toBeTruthy();
  });
});
